<script>$(document).ready(function() {
    $('#example').dataTable( {
        "language": {
            "url": "//cdn.datatables.net/plug-ins/1.10.16/i18n/Spanish.json"
        },
        "columnDefs": [ {
          "targets": 'no-sort',
          "orderable": false,
    } ] 
    } );
} );
</script>

<div class="bs-docs-section clearfix" style="margin-top: -4em;">
        <div class="row">
          <div class="col-lg-12">
            <div class="page-header">
              <h1 id="navbars">Libros totales</h1>
            </div> 
<div class="bs-component">
<table id="example">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Autor</th>
      <th class="no-sort" width="10%">Opciones</th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
      <tr id="<%= book.id %>">
        <td><%= book.name %></td>
        <td><%= book.author %></td>
<td> <div class="row">
                                    <div class="dropdown" style="text-align: center;">
                                        <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown"> Opciones
                                        <span class="caret"></span></button>
                                        <ul class="dropdown-menu">
                                            <li><%= link_to 'Consultar', notes_unauth_path(book) %></li>
                                    </div>

                                </div></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>

</div>

          </div>
        </div>
      </div>
